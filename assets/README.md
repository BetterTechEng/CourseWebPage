Place your real image files here, matching the names referenced in the HTML:

- favicon.png              (recommended 32x32 or 64x64)
- og-image.jpg              (1200x630, used for social link previews)
- instructor-photo.jpg      (square photo, at least 320x320)
- testimonial-1.jpg          (small square headshot, real participant)
- testimonial-2.jpg          (small square headshot, real participant)

Already included (real files, not placeholders):
- ai-qa-copilot-prompts.pdf  — a working PDF with placeholder content.
  Replace the bracketed text inside it with your real 20 prompts before
  publishing (edit the source script if you kept it, or recreate it in
  any tool you prefer).
- og-pattern.svg             — the node/flow visual motif used in the hero
  and curriculum, provided as an SVG so you can adapt it into a real
  1200x630 og-image.jpg with your course title. This keeps your social
  preview visually consistent with the rest of the site.

Until you add the missing image files above, their <img> tags will show
broken-image placeholders in the browser — this won't break layout or
deployment.
